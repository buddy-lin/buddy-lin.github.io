# 林俊颖个人简历网站 · 全流程制作经验手册

> 范围：单页 HTML 营销/简历站，从 v3.0 迭代到 v4.6.11（共 10+ 版本）
> 定位：一套**可复用、可照做**的工程范式，而非逐日流水账

---

## 一、项目概况

| 维度 | 说明 |
|------|------|
| 形态 | 单页 HTML，滚动式长页 + 多模块(机构) |
| 技术栈 | HTML + Tailwind(CDN) + 内联 JS + lucide 图标 + 静态图片 `assets/` |
| 交互 | 图片/双图 Lightbox、视频弹窗、sticky 吸顶导航+滚动高亮、复制联系方式、PDF 导出 |
| 打印 | A4 竖版、`@media print` 分页控制、保色、防截断 |
| 移动端 | 响应式 Tailwind、多宽度防溢出 |
| 部署 | GitHub Pages + WorkBuddy 沙箱双环境 |

**版本主线**：v3.0(文案/头像/证图/捏合缩放) → v4.0(去装饰图标+置顶导航+精简看板) → v4.1(看板调整+双图stack+滚动高亮) → v4.2(导航重排+吸顶) → v4.3~v4.6.4(打印排版、锚点、折叠) → v4.6.5~v4.6.11(文案、3+2看板、防溢出、图标、视频封面、导航高亮阈值)。

---

## 二、部署架构（关键决策）

- **双环境**：GitHub Pages(权威源头) + WorkBuddy 沙箱(可临时预览)。改动基本只改 `index.html`。
- **顶级 Pages 域名**：GitHub 用户名改 `buddy-lin`、仓库名改 `buddy-lin.github.io` 后，得到干净顶级地址 `https://buddy-lin.github.io/`。改名流程用 REST API `PATCH /repos/{user}/{repo}` + `{"name":...}`，本地 `git remote set-url` 用带 PAT 的 URL。
- **本地 git remote**：`https://<PAT>@github.com/buddy-lin/buddy-lin.github.io.git`（PAT 直接内嵌）。
- **验证线上务必用 raw 而非页面**：`raw.githubusercontent.com/buddy-lin/buddy-lin.github.io/main/index.html` 反映真实已推送内容；`buddy-lin.github.io` 会命中 CDN/浏览器缓存显示旧版。**`grep -c` 返回 0 时 exit code 是 1，脚本需注意**。
- **CDN 缓存误判**：用户看到旧版 = 浏览器/CDN 缓存，**Ctrl+Shift+R 强制刷新**即可，不要误以为推送失败。

---

## 三、可复用的核心调试工作流（本次最大价值）

### 1. 打印分页调试（排 PDF 空白/拥挤/折叠）
```
本地静态服务(python -m http.server) + Chrome headless --print-to-pdf
 → pymupdf 把 PDF 转 PNG，逐页核查内容分布与空白
 → 用 pymupdf 文本坐标(get_text("dict")) 定量算剩余空间 vs 整块高度
```
- 判断"能否挤进下一页"：先量**剩余可用空间**，再量**整块盒子高度**（含边框/margin），差额决定是否 `break-inside:avoid` 导致整块推页。
- **分页控制三件套**：`break-inside:avoid`(单内容块，勿整格避免误推页)、`print-break-page`(强制整块从新页开始，`page-break-before:always`)、`@media print` 内收敛行距/间距(如 `.timeline-item ul li margin-bottom:0.35rem`)。
- **版面调节模块标题大标题**：防"标题孤悬页尾" → 给该模块外层加 `print-break-page`。
- **细节**：`print-color-adjust:exact` 保留底色/品牌色；打印隐藏 `no-print` 工具条/底部 CTA。
- **HTML 标签配对校验**：数 `div/section/nav/button/ul/a` 开闭是否一对一（如 div 178/178、section 6/6）。
- **pymupdf 环境**：`pip install --target` 装后需 `sys.path.insert`，Git Bash 下 `PYTHONPATH` 传参异常。
- **Chrome headless 缓存**：分页没生效先怀疑磁盘缓存，加 `--disable-http-cache` + URL 时间戳参数。

### 2. 移动端横向溢出检测（左右晃动）
- **根因多在 flex 不换行 + shrink-0**：左右组总宽超容器。修复 = 容器 `flex flex-wrap` + 一侧 `min-w-0` 允许收缩。
- **CDP 多宽度批量检测**（可复用技能）：
```
Chrome --headless=new --disable-gpu --no-sandbox --remote-debugging-port=9223 \
  --remote-allow-origins=* --user-data-dir=_tmp_chrome
Python websocket-client: create_connection(suppress_origin=True)
 Emulation.setDeviceMetricsOverride 设 320/360/375/390/414/430
 Runtime.evaluate 遍历所有元素，比较 getBoundingClientRect().right 与 window.innerWidth
```

### 3. 导航滚动高亮 / 锚点跳转
- **判定命中错位根因**：`scroll-margin-top` 必须 ≤ `headerOffset`（即停靠点能被判定命中）。`scroll-margin-top:64 > headerOffset:52` 会错位。
- **修法三管齐下**：`scroll-margin-top` 调大(74) + `headerOffset` 调大(82 > 74) + 抽 `setActive()` 并给每个 `nav-link` 加 click 监听**点击立即高亮**，解除对滚动判定的依赖。
- **滚动进度阈值高亮**：`progress = (headerOffset - top)/h`，`progress>=0.7` 提前高亮下一个板块；`curIdx<0`(未滚到第一板块)时默认高亮 `sections[0].id`(导航吸顶即选中第一项)。
- **DOM 顺序 ≠ anchorIds 顺序**：sections 数组一定要按 `getBoundingClientRect().top` 排序后再运算，否则逻辑错乱。
- **精确浮点滚动点**：用 `round(at-82+p*h)` 而非 `int()` 取整，避免 70% 那档 progress 差临界一丝。

### 4. 图片压缩到 180KB 内（PIL 二分缩放 + JPEG）
- 策略：转 RGB → 二分缩放(scale 0.05~1.0) + JPEG `quality=85, optimize, progressive`，找**能 ≤ 目标字节的最大 scale**（保清晰度）。
- **必须保留原文件名**，页面引用才不受影响。
- 统一压到 ~170KiB(174080B) 留余量，避免正好贴 180KiB 边缘在不同工具里"看起来超了"。
- 子目录(如 `case_pdf_pages/`、`outlet/`)也要遍历。

### 5. 视频静态封面 + 引导播放
- **取帧**：`imageio` + `imageio-ffmpeg`(自带 ffmpeg 二进制) `get_reader('ffmpeg')` 按帧号取中间帧，`Image.fromarray` 存 PNG 再压 JPEG。
- **避免自动播放拦截黑屏**：`<video controls poster=...>` + `openVideoLightbox` 改为**不自动 play**，显示封面 + 居中圆形播放按钮遮罩；`playLightboxVideo()` 点击播放并隐藏遮罩；绑定 `play/pause` 事件同步遮罩显隐。
- 符合主流浏览器"有声音自动播放会被拦"策略。

### 6. 内联 JS 语法校验
```
node -e: 用正则 /<script(?![^>]*src)[^>]*>([\s\S]*?)<\/script>/gi
 提取所有内联 script，new Function(m[1]) 逐一 try/catch
```

---

## 四、CDP 与 Windows 调试踩坑速查

| 坑 | 解决 |
|----|------|
| websocket-client 连不上 | Chrome 需 `--remote-allow-origins=*`，Python 侧 `create_connection(suppress_origin=True)` |
| `Runtime.evaluate` 取不到值 | 返回结构是 **`resp["result"]["result"]["value"]`(两层 result)**，要取两层 |
| `awaitPromise:true` 返回 None | 读普通值**勿加 awaitPromise**(非 Promise 表达式会返回 None) |
| `/tmp` 路径无效 | Windows Python 下用工作目录相对路径 |
| `_tmp_chrome` 删不掉 | **先杀 Chrome 进程**再删目录；服务端口也要用 `Get-NetTCPConnection` 找 PID 杀 |
| grep -c 返回 0 报错 | exit code 为 1，脚本需处理；验证线上用 raw |

---

## 五、方法论（软经验 / 协作准则）

1. **先读源码再动手**：任何改动前完整读相关文件，禁止凭空实现或"做样子"。
2. **数据驱动，不靠猜**：分页用坐标定量、溢出用 CDP 定量、锚点用逻辑推演——用数字证明，不用"我感觉好了"。
3. **每次改动带版本号 + 完整提交信息**：`v4.6.11: 导航吸顶即选中非线智能(AI)` + 改动要点 + 验证方法，可回溯。
4. **交付前必须自行验证**：CDP 截图 / 边界滚动点 / 标签配对 / JS 语法 / raw 线上核对，逐项过。
5. **验证结论用表格**：当前板块/进度/active 状态列表格、宽度档位/溢出列表格，清晰可复核。
6. **外部动作克制**：域名改名、仓库重命名等用 REST API 且先确认；改动前若涉及个人文件(Desktop/Downloads)严守只读报告。
7. **知识沉淀**：CDP 调试溢出已沉淀为技能 `cdp-overflow-probe`，改名流程沉淀为 `github-pages-top-level-domain`；遇到反复用的工作流就落成技能，别每次重推。

---

## 六、微信内"非官方网页"提醒（重要认知）

- **根因**：微信对**非 `mp.weixin.qq.com` 的第三方域名**在内置浏览器打开时都弹"非微信官方网页"确认框。这是**微信客户端级拦截**，**任何网页代码都无法绕过**。
- `buddy-lin.github.io` 属于 GitHub 域名，无法被微信认可，也无法做公众号业务域名白名单(需域名所有权验证，github.io 提供不了)。
- 用户实测唯一外链是 `mp.weixin.qq.com/s/...`，微信内打开**不弹**。
- **可行方案排序**：
  1. 公众号推文链接(零成本、现在可用、不弹)——但**交互大幅降级**(失去弹窗/复制/滚动/导航，变成静态长图)。
  2. 备案域名 + 认证公众号白名单(需企业/组织主体，个人做不了)。
  3. 微信小程序(个人可做、体验最好、完全不弹、认证约300元)——代价是需要重做成小程序。
- **结论**：为省一个确认弹窗砍掉网站全部交互不划算；若核心诉求是"微信里体验最好"，小程序是最优解。

---

## 七、微信静默部署实战（2026-09 实操，独立域名方案【非完全可行】）

> ⚠️ **重要修正**：本次用 **独立域名 + 海外服务器 + 合规 HTTPS** 方案，**首次能打开，但后续微信又提示「该网站未完成ICP备案，无法确认其安全性」**。这说明**海外服务器+独立域名+HTTPS ≠ 一定能微信静默打开**。微信除「非官方网页」外，还有**另一层更严格的管控：对未完成 ICP 备案的域名，无论服务器在海外还是国内，都提示「未完成ICP备案」**。

### 结论先行
- **法律层面**：海外/HK 服务器托管，**确实不需要**中国大陆 ICP 备案（只有在大陆服务器托管才强制备案）。所以「我海外网站怎么还需要备案」的疑问是合理的——**法律上不需要**。
- **微信校验层面**：微信有一套**独立于法律**的风险校验机制。它会判断域名是否备案，**未备案域名（即使服务器海外）也会被标记为高风险**，提示「未完成ICP备案，无法确认其安全性」。这**不是法律强制，是微信自己的信任评级**。
- **首次成功/后续拦截**：第一次打开成功，可能因为微信对该域名的信任评分还没刷新或域名较新、未进入拦截名单；之后再次打开，微信安全检测触发，就提示未备案。**这不是稳定行为，不可依赖。**

### 根本解
- 面向**中国大陆用户**、要**稳定**微信静默打开，唯一可靠路线是：**大陆服务器 + 完成 ICP 备案**（需要大陆主体，个人/企业证件）。
- 独立域名 + 海外 + HTTPS 是「能上线一个真实网站」的合法路径，**但微信可能仍提示未备案**。属于「比 github.io 好，但不保证静默打开」。

### 关键决策点
1. **独立域名优于共享子域名**：`github.io` 共享子域名信任度低必弹；独立域名能降低"非官方网页"概率，**但不解决未备案提示**。
2. **子域名方案优于路径**：`buddy-lin.resumer.me` 干净独立入口；`resumer.me/buddy-lin` 会和主站混在一起，微信按整体域名判断。
3. **香港服务器无需 ICP 备案**：网站托管在大陆服务器才需备案，香港/UCloud 免备案，出海部署首选。
4. **域名注册商与服务器商可跨平台**：域名在阿里云买、服务器在 UCloud 买，两者能搭配（DNS 在阿里云配解析，服务器在 UCloud 跑），只是登录、解析入口不同。

### 可复用部署步骤（已实跑成功）
1. **服务器装环境**：`apt install nginx git certbot python3-certbot-nginx`。
2. **拉网站**：`git clone <公开仓库> /var/www/html`（注意先 `mkdir -p /var/www/html`，且目录损坏就先 `rm -rf` 再 clone）。
3. **配 nginx**：写 `sites-available/resumer`，`server_name buddy-lin.resumer.me resumer.me; root /var/www/html; index index.html; location / { try_files $uri $uri/ =404; }`，软链到 `sites-enabled`，`rm -f sites-enabled/default`，`nginx -t && systemctl reload nginx`。
4. **签证书**：`sudo certbot --nginx -d buddy-lin.resumer.me -d resumer.me --agree-tos -m <邮箱> --redirect`。

### 排障速查（本次踩坑）
| 现象 | 根因 | 解决 |
|------|------|------|
| nginx 配好却 404 | `/var/www/html` 目录不存在（被误删） | `mkdir -p` + 重新 clone |
| certbot 报 `unrecognized arguments: --non-interactle` | tty 粘贴长命令截断参数 | 去掉 `--non-interactive` |
| certbot 提示"已有证书未到期" | 证书其实还在，只是 `/live/` 软链丢失 | 选 **1: 重装现有证书**，不重签（省事且不触发 CA 限流） |
| 服务器 `apt` 卡 10 分钟无响应 | 下载/交互阻塞 | 拆成**单条短命令**独立跑，避免长 chain |
| 证书到期 | Let's Encrypt 90 天 | certbot 自动续期（systemd timer）**已自动，无需手动** |

### 服务器登录方式（关键限制）
- **公网 SSH 直连不可用**：UCloud 轻量服务器对公网 22 端口做了**网络层隔离/防护**——TCP 22 能通，但 SSH banner 交换超时（paramiko / OpenSSH verbose / 原生 socket 均失败），密码、权限、`ListenAddress 0.0.0.0`、防火墙全配对也无法突破。**这不是配置问题，agent 无法直接操作这台机器。**
- **用 UCloud 黑屏 tty 登录**（控制台 `dws.ucloud.cn`）：
  - 仅支持**键盘打字**，**不支持粘贴**，密码输入**不显示**（安全机制），**不支持 Ctrl+C**。
  - 登录：`ubuntu` 用户 + 密码，登录后 `sudo` 免密。
  - **命令必须极短单条**，避免长 chain / heredoc，否则粘贴循环重放刷屏。

### 本机 DNS 被劫持的注意点
- 用户本机若装了 **Cloudflare WARP/科学上网**，会把 `resumer.me` 劫持成 `198.18.0.x` 保留地址，导致**自己电脑直接访问域名异常**。
- **不影响微信和外部用户**（他们走真实 DNS）。本地想正常访问：关代理，或在 `hosts` 加 `128.1.131.208 buddy-lin.resumer.me`。
